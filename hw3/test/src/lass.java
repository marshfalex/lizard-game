import java.util.ArrayList;

public class lass {
    String result = "";
    for(int i = 0; i < str.length; i++){
        char c = str.charAt(i);
        if(c == "a" || c == "e" || c == "i" || c == "o" || c == "u"){
            c = "_";
        }
        result += c;
    }
}return result;

ArrayList<Integer> noDupsList = new ArrayList<Integer>();

for(int i = 0; i < dupsArr.length; i++){
    if(!noDupsList.contains(dupsArr[i])){
        noDupsList.add(dupsArr[i]);
        }
        }

int[] noDupsArr = new int[noDupsList.size()];
for(int i = 0; i < noDupsList.size(); i++){
    noDupsArr[i] = noDupsList.get(i);
        }


ArrayList<String> palindromeList = new ArrayList<String>();

File file = new File(filename);
Scanner scanner;

try{
    scanner = new Scanner(file);
        }

while(scanner.hasNext()){
    String word = scanner.next();
    if(isPalindrome()){
        palindromeList.add(word)
        }
        }


int[][] shiftTable = new int[arr.length][arr.length];
for(int i = 0; i < arr.length; i++){
    for(int j = 0; j < arr.length; j++){
        shiftTable[i][j] = arr[]
        }
        }





File file = new File(filename);
Scanner scan = new Scanner(file);
while(scanner.hasNext()){
        }